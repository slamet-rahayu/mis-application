spring.application.name=spring-boot
server.port=8081

## default connection pool
spring.datasource.hikari.connectionTimeout=20000
spring.datasource.hikari.maximumPoolSize=5

## PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/mis_application
spring.datasource.username=postgres
spring.datasource.password=jenengmu
spring.datasource.driver-class-name=org.postgresql.Driver

# create and drop table, good for testing, production set to none or comment it
#spring.jpa.hibernate.ddl-auto=update

#JWT
jwt.secret=hello12345
jwt.expiration.month=1

# image upload
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=2MB
spring.servlet.multipart.max-request-size=2MB
upload.path=src/main/resources/static/uploads

#jsp
mvc.view.prefix=/WEB-INF/views/
mvc.view.suffix=.jsp

# Set the root logging level
logging.level.root=INFO
# Set logging level for a specific package
logging.level.com.example=DEBUG
# Set logging level for Spring framework
logging.level.org.springframework=WARN

api.app.integration=http://localhost:8082/summary
